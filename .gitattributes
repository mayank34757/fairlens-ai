# Normalize line endings for all text files to LF on commit (cross-platform consistency)
* text=auto eol=lf

# Force these binary files to not be text-diffed
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text eol=lf
*.woff binary
*.woff2 binary
